[build]
builder = "nixpacks"

[deploy]
startCommand = "pnpm --filter @workspace/api-server run live -- --strategy=fractal_breakout --interval=1h --leverage=20 --risk=10 --capital=100 --symbol=BTCUSDT"
restartPolicyType = "always"
restartPolicyMaxRetries = 10
